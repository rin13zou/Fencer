<div>
  <p><%= @post.user.user_name %>：<%= image_tag @post.user.get_profile_image(100,100) %></p>
  <p><%= @post.body %></p>
  <p>投稿日：<%= @post.created_at.strftime('%Y/%m/%d') %></p>
  <% if @post.user == current_user %>
    <%= link_to "削除", post_path(@post), method: :delete %>
  <% end %>
</div>